# maplibre-gl@5 pulls a build-time style-spec dependency that declares node ">= 22"; the module runs
# fine in the browser bundle on the deploy node runtime, so skip the engine gate to keep installs green.
ignore-engines true
