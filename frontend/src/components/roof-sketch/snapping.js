// Thin compatibility wrapper (Phase B1B). Snap logic is authoritative in @roofspan/roof-sketch-core.
// No local snap/projection formulas remain here.
export { modelTolerance, snapTarget } from "@roofspan/roof-sketch-core";
